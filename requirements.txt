fastapi==0.104.1
python-multipart==0.0.6
uvicorn==0.24.0
python-dotenv==1.0.0
anthropic==0.111.0
psycopg2-binary==2.9.12
# ✅ แก้ 2026-07-04: เดิม 2.0.23 (ก่อน Python 3.13/3.14 จะออกด้วยซ้ำ) ชนกับ Python 3.14 ของ MBBook
# ในเครื่อง (AssertionError: TypingOnly) ทำให้รัน pytest ในเครื่องไม่ได้มาตั้งแต่ 2026-07-03
# 2.0.51 คือเวอร์ชันล่าสุดในสาย 2.0.x (ไม่ใช่ 2.1 beta) ที่รองรับ Python 3.14 เต็มที่
sqlalchemy==2.0.51
# ✅ แก้ 2026-07-04: httpx ไม่เคย pin เวอร์ชันไว้เลย ตอน pip install ใหม่ได้ 0.28+ มา
# ซึ่งเอา Client(app=...) ออกไปแล้ว แต่ fastapi==0.104.1 (starlette เก่า) TestClient ยังเรียกแบบนั้นอยู่
# → TypeError: Client.__init__() got an unexpected keyword argument 'app' (23 tests fail)
# 0.27.2 คือเวอร์ชันสุดท้ายที่ยังรองรับ ใช้กับ anthropic==0.111.0 ได้ปกติ ไม่กระทบ production
httpx==0.27.2
pydantic==2.13.4
yfinance==0.2.32
pandas==3.0.3
apscheduler==3.10.4
pytz==2024.1
requests-ratelimiter