# Handoff Archive — สถานะ ณ 2026-07-05 (ก่อนงาน 2026-07-08)

> ✅ 2026-07-08: `Handoff.md` ย่อให้เหลือแค่กติกา/ข้อจำกัดที่ยังใช้จริงแล้ว — เนื้อหาที่บรรยายสถานะ UI ณ
> ตอนนั้น (2026-07-05) ย้ายมาเก็บที่นี่ทั้งหมด **ล้าสมัยไปแล้ว** (ถูกแทนที่ด้วยงาน 2026-07-08 — portfolio-
> hero layout, change_pct, ลบปุ่มลบ) เก็บไว้อ้างอิงประวัติเท่านั้น อย่าใช้เป็นสถานะปัจจุบัน

---

## บริบทงานตอนนั้น (10 รอบแก้ไข ต่อจาก rebuild ใหญ่ก่อนหน้า)

หลัง rebuild `App.jsx` ทั้งไฟล์ตาม `UI_Spec.md` เดิม MBBook ทดสอบจริงแล้วทยอยขอแก้เป็นรอบๆ:
รอบ 5 (`/scrutinize` เจอ 8 finding, ตัดสิน "ทำตาม mockup 100%" กลับไปใช้ตาราง Desktop), รอบ 6 (เพิ่ม font
Desktop + กราฟสะสมเป็นเส้นทุก tab), รอบ 7-9 (ปรับ font หน้า News หลายรอบ, เพิ่มป้าย New + ปุ่มอ่านทั้งหมด),
รอบ 10 (ปุ่มทดสอบข่าว) — รายละเอียดเต็มอยู่ใน `Pending_Archive.md`

## สถานะ UI ณ ตอนนั้น (ล้าสมัยแล้ว — ดู Pending.md 2026-07-08 แทน)

- Desktop Portfolio เดิมเป็น 3 คอลัมน์เรียงเต็มความกว้าง (ไม่ใช่ portfolio-hero 70:30 แบบตอนนี้)
- ใช้ `DesktopValueBarChart` แสดงมูลค่า ฿K จริง (ถูกลบไปแล้ว 2026-07-08 — Desktop ใช้กราฟ % เหมือน Mobile)
- Tickers การ์ด/แถวยังมีปุ่มลบถังขยะ (ถูกลบไปแล้ว 2026-07-08 — ลบได้ทาง popup อย่างเดียว)
- คอลัมน์ "เปลี่ยนแปลง" ใน Tickers ยังว่างเปล่า (เพิ่ม `change_pct` จริงแล้ว 2026-07-08)
- News tab ยังเป็น MOCK DATA ทั้งหมด (ยังจริงอยู่จนถึงตอนนี้ — backend #51 ยังไม่สร้าง)
- System tab: กราฟต้นทุนระบบ, Win rate progress bar, "รายงานจากนิก" — โครงยังเหมือนเดิม ไม่เปลี่ยน

## Mock/Gap ที่บันทึกไว้ตอนนั้น (อัพเดตสถานะ 2026-07-08)

| จุด | สถานะตอนนั้น (07-05) | สถานะล่าสุด (07-08) |
|---|---|---|
| ข่าวทั้งหมด (`MOCK_NEWS`) | Mock data, รอ backend #51 | ยังเป็น mock อยู่ (ไม่เปลี่ยน) |
| ปุ่ม "+ ข่าวทดสอบ" | Dev tool ชั่วคราว | ยังอยู่ (ผูกกับ #51) |
| Win rate historical trend | รอข้อมูลสะสม (#53) | ยังไม่ทำ (ไม่เปลี่ยน) |
| PEG ratio field | ยังไม่ยืนยัน 100% | **ยืนยันแล้ว 07-08**: `peg_ratio` เป็น `null` ทุก ticker จริง (ดู Pending.md รายการค้าง) |
| Per-holding day-change % | ยังไม่มี previous-close เก็บแยก | **✅ ทำเสร็จแล้ว 07-08** — `change_pct` จาก `signal_history` |
| `company_name`/`company_description` | เพิ่ม backend แล้ว รอ deploy | ✅ deploy นานแล้ว ทำงานปกติ |

## คำถามที่เคยตั้งไว้เผื่อจะรื้อ UI ใหม่ทั้งหมด (ไม่เคยเกิดขึ้นจริง — แนวทางที่เกิดจริงคือพอร์ต mockup
เข้าไปทีละส่วนแบบ evolutionary แทน ไม่ได้รื้อทั้งไฟล์)

- จะรื้อโครงสร้างไฟล์ด้วยไหม (แตกเป็นหลายไฟล์ vs single-file เดิม) — **คำตอบที่เกิดขึ้นจริง**: เริ่มแยกบาง
  ส่วนแล้ว 2026-07-08 (`constants.js`) แบบระมัดระวังเฉพาะส่วนเสี่ยงศูนย์ ไม่ใช่รื้อทั้งไฟล์
- Design system เดิม (navy/purple/gold) จะใช้ต่อหรือเปลี่ยนใหม่ — **คำตอบที่เกิดขึ้นจริง**: ใช้ต่อ ไม่เคย
  เปลี่ยน
